#include "monty.h"
/**
  *f_nop- where no operation is present
  *@head: stack head
  *@line_number: integer of line numbers
  *Return: no return
 */
void f_nop(stack_t **head, unsigned int line_number)
{
	(void) line_number;
	(void) head;
}
