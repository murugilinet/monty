#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @line_number:where linw is
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	bus.flag_c = 0;
}
